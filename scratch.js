let start = $.now();
        
console.log(start)

if (sentencesCount == j + 1) {
    if (sentencesL == k + 1) {
        let d = new Date();
    let time2 = d.getTime();
    let finish = Math.round(time2 / seconds);
    }
}
let finish = Math.round(time2 / seconds);

$('#prompt-container').append()